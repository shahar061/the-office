import type { ProjectState } from '@shared/types';
export default function ProjectPicker({ onProjectOpened }: { onProjectOpened: (state: ProjectState) => void }) {
  return <div>ProjectPicker placeholder</div>;
}
